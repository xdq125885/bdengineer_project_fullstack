## 🎯 快速运行指南

### 前置条件

✅ Python 3.8+  
✅ 已安装依赖 (`pip install -r requirements.txt`)  
✅ 有GPU更佳（可选，但推荐）

---

## 📌 三步快速开始

### 第1步：准备数据

你已经有了三个关键文件：

```
✓ 用户登录.md          - PRD文档（需求）
✓ PRDAI1.json         - AI生成的测试用例
✓ prdrengong.json     - 人工编写的参考用例
```

### 第2步：安装依赖

```bash
pip install -r requirements.txt
```

### 第3步：运行演示

```bash
python demo.py
```

---

## 📊 运行结果

演示脚本会自动：

1. **加载数据**
   - 读取PRD文档
   - 加载AI生成的用例（43个）
   - 加载人工编写的用例（17个）

2. **执行评测**
   - 评测AI生成用例
   - 评测人工编写用例
   - 对比两个版本

3. **生成报告**
   - JSON格式报告（详细数据）
   - HTML格式报告（可视化）
   - 文本摘要报告

4. **输出位置**
   ```
   data/evaluation_results/demo_results/
   ├── ai_evaluation_report.json       # AI用例详细报告
   ├── ai_evaluation_report.html       # AI用例可视化报告
   ├── human_evaluation_report.json    # 人工用例详细报告
   ├── human_evaluation_report.html    # 人工用例可视化报告
   └── version_comparison.json         # 版本对比报告
   ```

---

## 📈 查看报告

### 方式1：查看HTML报告（推荐）

```bash
# Windows
start data/evaluation_results/demo_results/ai_evaluation_report.html

# macOS
open data/evaluation_results/demo_results/ai_evaluation_report.html

# Linux
xdg-open data/evaluation_results/demo_results/ai_evaluation_report.html
```

### 方式2：查看JSON报告

```bash
# 查看AI用例评测结果
cat data/evaluation_results/demo_results/ai_evaluation_report.json

# 查看版本对比结果
cat data/evaluation_results/demo_results/version_comparison.json
```

### 方式3：查看日志输出

```bash
# 查看演示日志
cat logs/demo.log

# 查看评测日志
cat logs/evaluation.log
```

---

## 🔍 理解输出

### 综合分数

```
AI生成用例综合分数:    0.7234
人工编写用例综合分数:  0.8456
差异:                 0.1222
```

**解读：** 人工编写的用例质量比AI生成用例高12.22%

### 各维度分数

```
指标              AI生成      人工编写      差异
结构完整性        0.7500      0.8200      -0.0700
内容质量          0.6800      0.8500      -0.1700
去重性            0.9100      0.9500      -0.0400
覆盖率            0.6500      0.8200      -0.1700
相似度            0.7800      N/A         N/A
```

**解读：**
- ✓ AI用例的去重性最好（0.91）
- ✗ AI用例的内容质量最差（0.68）
- 💡 需要改进：内容质量和覆盖率

---

## 🛠️ 自定义使用

### 使用自己的数据

#### 方式1：修改demo.py

```python
# 在demo.py中修改这三行
prd_file = "你的PRD文件.md"
ai_cases_file = "你的AI用例.json"
human_cases_file = "你的参考用例.json"

# 然后运行
python demo.py
```

#### 方式2：使用命令行工具

```bash
# 评估单个版本
python main.py evaluate your_cases.txt \
    -r reference_cases.txt \
    -p prd.txt \
    -o ./my_results

# 对比两个版本
python main.py compare \
    version1.txt \
    version2.txt \
    -r reference_cases.txt \
    -p prd.txt \
    -o ./my_results
```

#### 方式3：Python代码集成

```python
from evaluation.evaluator import Evaluator
from evaluation.utils import FileUtils

# 初始化
evaluator = Evaluator(use_similarity_model=True)

# 加载数据
generated = FileUtils.read_text("generated.txt").split('\n\n')
reference = FileUtils.read_text("reference.txt").split('\n\n')
prd = FileUtils.read_text("prd.txt")

# 评测
results = evaluator.evaluate_batch(generated, reference, prd)

# 查看结果
print(f"综合分数: {results['overall_score']:.4f}")
```

---

## 📝 输入文件格式

### PRD文件格式（Markdown）

```markdown
# 产品名称

## 目标
系统的目标描述

## 业务规则
- 规则1
- 规则2
- 规则3

## 用例示例
- 场景1
- 场景2
```

### 测试用例文件格式（Markdown）

```markdown
# 用例标题1

## 前置条件
- 条件1
- 条件2

## 操作步骤
1. 步骤1
2. 步骤2
3. 步骤3

## 预期结果
- 结果1
- 结果2

---

# 用例标题2
...
```

### 测试用例文件格式（JSON）

```json
[
  {
    "id": "TC001",
    "title": "用例标题",
    "preconditions": ["条件1", "条件2"],
    "steps": ["步骤1", "步骤2"],
    "expected": ["结果1", "结果2"]
  },
  {
    "id": "TC002",
    "title": "用例标题2",
    ...
  }
]
```

---

## 🚀 进阶用法

### 1. 调整评测权重

编辑 `evaluation/config.py`：

```python
METRIC_WEIGHTS = {
    "structure": 0.15,      # 结构完整性权重
    "coverage": 0.35,       # 覆盖率权重（提高）
    "quality": 0.25,        # 内容质量权重
    "similarity": 0.15,     # 相似度权重
    "uniqueness": 0.10,     # 去重性权重
}
```

### 2. 使用GPU加速

编辑 `evaluation/config.py`：

```python
MODEL_CONFIG = {
    "encoder_model": "sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2",
    "device": "cuda",       # 改为 "cuda" 使用GPU
    "batch_size": 64,       # 增加批处理大小
}
```

### 3. 自定义日志级别

```python
from evaluation.utils import Logger

logger = Logger.setup_logger(
    "my_app",
    log_file="my_app.log",
    level="DEBUG"  # 改为 DEBUG 获取更详细的日志
)
```

---

## ⚠️ 常见问题

### Q: 运行demo.py时出错？

**A:** 检查以下几点：

```bash
# 1. 确认依赖已安装
pip list | grep -E "torch|sentence-transformers"

# 2. 确认数据文件存在
ls -la *.md *.json

# 3. 查看日志
cat logs/demo.log

# 4. 重新安装依赖
pip install -r requirements.txt --upgrade
```

### Q: 运行很慢？

**A:** 这是正常的，因为需要加载预训练模型。首次运行会下载模型（~400MB）。

```bash
# 使用GPU加速（如果有GPU）
# 编辑 evaluation/config.py，改 device 为 "cuda"

# 或者禁用相似度模型（快但准确性降低）
evaluator = Evaluator(use_similarity_model=False)
```

### Q: 如何使用我自己的数据？

**A:** 有三种方式：

```bash
# 方式1：修改demo.py中的文件路径
# 方式2：使用命令行工具
python main.py evaluate your_file.txt -r reference.txt -p prd.txt

# 方式3：写Python脚本调用API
```

### Q: 报告在哪里？

**A:** 所有报告都在这个目录：

```
data/evaluation_results/demo_results/
```

用浏览器打开 `.html` 文件查看可视化报告。

---

## 📊 示例输出

### 日志输出

```
================================================================================
自动化测评系统演示 - 用户登录功能
================================================================================

【步骤1】加载数据
--------------------------------------------------------------------------------
读取PRD文件: 用户登录.md
PRD长度: 245 字符

加载AI生成的用例: PRDAI1.json
AI生成用例数: 43

加载人工编写的用例: prdrengong.json
人工编写用例数: 17

【步骤2】初始化评测器
--------------------------------------------------------------------------------
初始化评测器（包括相似度模型）...
评测器初始化完成

【步骤3】评测AI生成的用例
--------------------------------------------------------------------------------
执行评测...
AI用例评测完成

【步骤4】评测人工编写的用例
--------------------------------------------------------------------------------
执行评测...
人工用例评测完成

【步骤5】版本对比分析
--------------------------------------------------------------------------------
对比AI生成用例与人工编写用例...
版本对比完成

【步骤6】生成报告
--------------------------------------------------------------------------------
✓ AI用例评测报告: data/evaluation_results/demo_results/ai_evaluation_report.json
✓ 人工用例评测报告: data/evaluation_results/demo_results/human_evaluation_report.json
✓ 版本对比报告: data/evaluation_results/demo_results/version_comparison.json
✓ AI用例HTML报告: data/evaluation_results/demo_results/ai_evaluation_report.html
✓ 人工用例HTML报告: data/evaluation_results/demo_results/human_evaluation_report.html

【步骤7】对比摘要
--------------------------------------------------------------------------------

📊 【综合分数对比】
  AI生成用例综合分数:    0.7234
  人工编写用例综合分数:  0.8456
  差异:                 0.1222

📈 【各维度分数对比】
指标                 AI生成          人工编写          差异
-----------------------------------------------------------------
结构完整性           0.7500          0.8200          -0.0700
内容质量             0.6800          0.8500          -0.1700
去重性               0.9100          0.9500          -0.0400
覆盖率               0.6500          0.8200          -0.1700
相似度               0.7800          N/A             N/A

🔍 【详细分析】

  AI生成用例去重性:
    - 完全重复: 2
    - 高度相似: 5
    - 多样性分数: 0.8300

  人工编写用例去重性:
    - 完全重复: 0
    - 高度相似: 1
    - 多样性分数: 0.9500

💡 【改进建议】
  AI生成用例总体质量低于人工用例 14.6%
  1. 重点改进: 内容质量 (当前分数: 0.6800)
  2. 增加用例多样性，减少重复
  3. 扩大需求覆盖范围
  4. 提高用例描述的清晰度和具体性

================================================================================
演示完成！所有报告已生成
输出目录: data/evaluation_results/demo_results
================================================================================
```

---

## ✅ 检查清单

运行前请确认：

- [ ] Python 3.8+ 已安装
- [ ] 依赖已安装 (`pip install -r requirements.txt`)
- [ ] 数据文件存在（用户登录.md, PRDAI1.json, prdrengong.json）
- [ ] 有足够的磁盘空间（~1GB用于模型）
- [ ] 网络连接正常（首次运行需要下载模型）

---

## 🎉 开始使用

```bash
# 一键运行演示
python demo.py

# 查看结果
open data/evaluation_results/demo_results/ai_evaluation_report.html
```

祝你使用愉快！有问题请查看日志文件。

---

**需要帮助？** 查看 `QUICKSTART.md` 获取更详细的信息。

